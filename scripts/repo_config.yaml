# Repos to include in the auto-generated GitHub Repositories section.
# Add repo names (not full URLs) one per line.
# Only public, non-fork repos will be shown regardless.
include:
  - KTANE-Bomb-Manual
